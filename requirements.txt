# Production dependencies
requests>=2.28.0

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# POC: Files On-Demand via FUSE (requires: sudo apt install fuse3 libfuse3-dev)
pyfuse3>=3.3.0
trio>=0.22.0
